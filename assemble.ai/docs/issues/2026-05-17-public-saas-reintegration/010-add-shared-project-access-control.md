# 010 - Add shared project access control

**Type:** AFK
**Triage category:** `enhancement`
**Triage label:** `ready-for-agent`
**Source PRD:** `docs/prds/2026-05-17-public-saas-reintegration-prd.md`
**User stories covered:** 37, 38, 56

## What to build

Create shared access-control helpers for authenticated user loading, workspace ownership, project access, writable access, upload access, AI access, and export exceptions. Migrate a representative set of project APIs to prove the helper shape before the full security pass.

## Agent Brief

> *This was generated by AI during triage.*

Consolidate the access-control checks introduced in issues 006-009 into a shared helper layer. The helper should load the Better Auth user, require a workspace, verify project ownership, and delegate to the entitlement/upload/export/AI gates where needed. Migrate representative routes only; the next issue handles the broad security sweep.

## Acceptance criteria

- [x] Shared helpers derive the current user from the Better Auth session.
- [x] Shared helpers can require project ownership before returning project data.
- [x] Shared helpers can require writable entitlement before mutations.
- [x] Shared helpers can allow read-only export/download exceptions for owned data.
- [x] At least one read route, one write route, one upload route, and one export/download route use the helpers.
- [x] Tests cover helper behavior and the migrated representative routes.

## Blocked by

- 004 - Add entitlement state and trial banner.
- 007 - Enforce project cap and workspace gates.

## Implementation notes

- Added shared project access helpers for authenticated workspace, read ownership, writable access, upload access, export/download access, and AI access.
- Migrated representative read/write routes (`/api/projects/[projectId]`), upload route (`/api/projects/[projectId]/invoices/upload`), and export/download routes (`documents/download`, `program/export`) to the shared helpers.
- Existing lower-level entitlement/document/AI gates remain the source of truth for limits and read-only exceptions.

## Verification

- `npm.cmd test -- --runTestsByPath src/lib/auth/__tests__/project-access.test.ts`
- `npm.cmd run lint -- src/lib/auth/project-access.ts src/lib/auth/__tests__/project-access.test.ts src/app/api/projects/[projectId]/route.ts src/app/api/projects/[projectId]/invoices/upload/route.ts src/app/api/projects/[projectId]/documents/download/route.ts src/app/api/projects/[projectId]/program/export/route.ts`
