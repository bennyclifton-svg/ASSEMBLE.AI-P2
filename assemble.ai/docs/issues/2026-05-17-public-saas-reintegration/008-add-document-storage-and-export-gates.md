# 008 - Add document storage and export gates

**Type:** AFK
**Triage category:** `enhancement`
**Triage label:** `ready-for-agent`
**Source PRD:** `docs/prds/2026-05-17-public-saas-reintegration-prd.md`
**User stories covered:** 15, 23, 35, 36, 40, 57

## What to build

Make Supabase Storage the production SaaS storage path for customer files and apply ownership, upload allowance, document cap, download, and export rules to the document flow. Expired users should keep download/export access while uploads are blocked.

## Agent Brief

> *This was generated by AI during triage.*

Keep this to a representative document-flow slice. Preserve the existing storage abstraction, make its SaaS preference explicit, and add entitlement gates before file writes or export generation. Uploads must verify authenticated project ownership and enforce trial document caps before calling storage or queueing workers. Downloads/exports should verify ownership but continue to allow expired read-only users.

## Acceptance criteria

- [x] Production SaaS storage configuration prefers Supabase Storage through the existing storage abstraction.
- [x] Upload APIs require authenticated project ownership and upload allowance before writing files.
- [x] Trial document caps are enforced before storage writes or worker jobs are queued.
- [x] Document downloads require ownership but remain allowed for expired read-only users.
- [x] Project/document export remains allowed for expired read-only users when the data belongs to them.
- [x] Tests cover upload allowed, upload over cap blocked, cross-user download blocked, expired-user download allowed, and expired-user upload blocked.

## Blocked by

- 004 - Add entitlement state and trial banner.
- 007 - Enforce project cap and workspace gates.

## Implementation notes

- Added a document upload/export gate that combines workspace ownership, upload/export allowance, and plan/trial document limits.
- Applied upload gates to invoice and variation upload routes before form parsing, AI extraction, storage writes, or worker-adjacent work.
- Applied export gates to document download and program export routes, preserving expired-user download/export access.
- Made Supabase storage preference explicit through a pure storage configuration helper used by the existing storage abstraction.

## Verification

- `npm.cmd test -- --runTestsByPath src/lib/subscription/__tests__/document-gates.test.ts src/lib/storage/__tests__/settings.test.ts src/lib/projects/__tests__/workspace-access.test.ts`
- `npm.cmd run lint -- src/lib/subscription/document-gates.ts src/lib/subscription/__tests__/document-gates.test.ts src/lib/storage/settings.ts src/lib/storage/__tests__/settings.test.ts src/lib/projects/workspace-access.ts src/app/api/projects/[projectId]/documents/download/route.ts src/app/api/projects/[projectId]/invoices/upload/route.ts src/app/api/projects/[projectId]/variations/upload/route.ts src/app/api/projects/[projectId]/program/export/route.ts`
