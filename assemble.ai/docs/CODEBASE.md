# Codebase Context

The current codebase context lives at the repository root:

[CODEBASE.md](../CODEBASE.md)

That file is the source of truth for current architecture, local/private setup, typed RFI implementation, action-only writes, AI memory, weekly report drafts, backup/restore, and documentation boundaries.

Older Assemble/SaaS-era context has been archived under `archive/` so it can be mined as history without being mistaken for current implementation guidance.
