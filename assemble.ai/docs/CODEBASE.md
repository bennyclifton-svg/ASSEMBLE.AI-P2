# Codebase Context

The current codebase context lives at the repository root:

[CODEBASE.md](../CODEBASE.md)

That file is the source of truth for the current public SaaS architecture, live `sitewise.au` deployment shape, Better Auth signup/trials, Polar billing, entitlement gates, SaaS health checks, workers, storage, AI/RAG, typed RFIs, action-only writes, AI memory, and deployment boundaries.

Local/private setup docs remain under `docs/setup/` for development use. Older Assemble/SaaS-era context has been archived under `archive/` so it can be mined as history without being mistaken for current implementation guidance.
